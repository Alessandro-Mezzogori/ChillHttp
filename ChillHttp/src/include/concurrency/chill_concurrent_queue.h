#pragma once

#include <chill_lock.h>
#include <chill_log.h>
#include <errno.h>

typedef struct _ConcurrentQueue ConcurrentQueue;

//errno_t cq_queue_init(ConcurrentQueue** queue);
//errno_t cq_queue_item(ConcurrentQueue* queue, void* item);
//void* cq_queue_pop(ConcurrentQueue* queue);


